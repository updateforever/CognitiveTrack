# Identity research tools

This directory contains an isolated future-research utility for constructing cross-sequence
same-class identity hard negatives. Identity classification is not part of the current v6.4
`bbox_2d/status/memory_update` SFT protocol, so these samples must never be mixed into the formal
tracking or state-update releases without a separately versioned protocol.
